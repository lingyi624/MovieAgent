<Window x:Class="MovieAgent.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Movie Agent"
        Width="500" Height="450"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Border CornerRadius="20" Background="#1A1A2E">
            <Grid Margin="30">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid Height="100" Width="100" Margin="0 0 0 15">
                        <Ellipse Fill="#E94560" Opacity="0.25" />
                        <Image Source="logo.svg" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <Ellipse Width="85" Height="85" 
                                 Stroke="#E94560" 
                                 StrokeThickness="2" 
                                 Fill="Transparent"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center">
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation 
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                From="0" To="360" 
                                                Duration="0:0:2" 
                                                RepeatBehavior="Forever" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                            <Ellipse.RenderTransform>
                                <RotateTransform CenterX="42.5" CenterY="42.5" />
                            </Ellipse.RenderTransform>
                        </Ellipse>
                    </Grid>

                    <TextBlock Text="MOVIE AGENT" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Foreground="#E94560" 
                               HorizontalAlignment="Center" Margin="0 0 0 10" />

                    <TextBlock Text="您的智能电影管家" 
                               FontSize="14" 
                               Foreground="#CCCCCC" 
                               HorizontalAlignment="Center" Margin="0 0 0 20" />

                    <TextBlock x:Name="StatusText" 
                               Text="正在初始化..." 
                               FontSize="14" 
                               Foreground="#AAAAAA" 
                               HorizontalAlignment="Center" Margin="0 0 0 20" />

                    <Grid Width="280" Height="8" Margin="0 0 0 10">
                        <Border Background="#2D2D44" CornerRadius="4" />
                        <Border x:Name="ProgressBar" Background="#E94560" CornerRadius="4" Width="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#E94560" BlurRadius="8" Opacity="0.6" />
                            </Border.Effect>
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation 
                                                Storyboard.TargetProperty="Width"
                                                From="0" To="280" 
                                                Duration="0:0:2.5" 
                                                RepeatBehavior="Forever" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                        </Border>
                    </Grid>
                </StackPanel>

                <TextBlock Text="© 2026 Movie Agent. All rights reserved." 
                           FontSize="10" 
                           Foreground="#555555" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Bottom" />
            </Grid>
        </Border>
    </Grid>
</Window>